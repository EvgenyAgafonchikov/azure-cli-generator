{
  "info": {
      "title": "Microsoft.Azure.Management.Network",
      "version": "3.1.0-preview",
      "description": "Network Resource Provider"
  },
  "produces": [
    {
      "name": "CLI",
      "flavor": "Verb"
    }
  ],
  "operations": [
    { "name": "PublicIPAddresses",
      "parameters": ["location", "domainNameLabel", "publicIPAllocationMethod", "idleTimeoutInMinutes", "reverseFqdn", "publicIPAddressVersion", "tags"],
      "required": ["location"]
    },
    { "name": "VirtualNetworks",
      "parameters": ["location", "addressPrefixes", "dnsServers", "tags"],
      "required": ["location"]
    },
    {
        "name": "Subnets",
        "parameters": ["addressPrefix", "networkSecurityGroupId", "networkSecurityGroupName", "routeTableId", "routeTableName"],
        "required": [],
        "parent": "virtualNetwork"
    },
    { "name": "LoadBalancers",
      "parameters": ["location", "tags"],
      "required": ["location"]
    },
    { "name": "NetworkSecurityGroups",
      "parameters": ["location", "tags"],
      "required": ["location"]
    }
  ]
}
